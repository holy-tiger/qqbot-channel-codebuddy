{
  "mcpServers": {
    "qq-channel": {
      "command": "${HOME}/.local/bin/qqbot",
      "args": [
        "channel",
        "-config",
        "${CODEBUDDY_PLUGIN_DATA}/config.yaml"
      ]
    }
  },
  "disabledMcpServers": [
    "qq-channel"
  ]
}
