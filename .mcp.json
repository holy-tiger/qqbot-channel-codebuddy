{
  "mcpServers": {
    "qq-channel": {
      "command": "${HOME}/.local/bin/qqbot",
      "args": [
        "channel",
        "-config",
        "${HOME}/.local/bin/qqbot-config.yaml"
      ]
    }
  }
}
